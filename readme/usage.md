After installation, you can include the shared properties of `pokedex-go` in your own TypeScript config file.

### Node

`YOURPROJECT/tsconfig.json`
```json
{
  "extends": "pokedex-go/base"
}
```

### React

`YOURPROJECT/tsconfig.json`
```json
{
  "extends": "pokedex-go/react"
}
```